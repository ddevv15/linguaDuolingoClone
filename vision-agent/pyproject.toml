[project]
name = "vision-agent"
version = "0.1.0"
description = "AI language teacher voice agent — OpenAI Realtime + Stream Edge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "vision-agents[getstream,openai]>=0.6.3",
]
